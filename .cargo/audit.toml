# Only unmaintained warnings are documented here.
# Crypto CVEs resolved by OpenMLS 0.8.1 upgrade (curve25519-dalek, ed25519-dalek).

[advisories]
ignore = [
    # paste is unmaintained but has no CVE — transitive from sqlx-core
    "RUSTSEC-2024-0436",
    # rustls-pemfile is unmaintained but has no CVE — transitive from sqlx-core and reqwest
    "RUSTSEC-2025-0134",
]
