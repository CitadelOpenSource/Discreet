Notification sounds are synthesized client-side via Web Audio API.
No audio files are downloaded from the server — zero network overhead.
See client/src/utils/sounds.ts for the synthesis definitions.

Variants: default, chime, pop, bell, none.
