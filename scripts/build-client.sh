#!/bin/bash
# ARCHIVED — Client build is handled by setup.sh (step 9).
# For manual build: cd client && npm ci && npm run build
# Delete with: git rm scripts/build-client.sh
echo "Manual client build: cd client && npm ci && npm run build"
exit 1
