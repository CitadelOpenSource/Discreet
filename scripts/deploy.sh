#!/bin/bash
# ARCHIVED — Production deployment is documented in GUIDE/DEPLOYMENT.md.
# Use scripts/generate-secrets.sh for production .env generation.
# Delete with: git rm scripts/deploy.sh
echo "See GUIDE/DEPLOYMENT.md for production deployment instructions."
echo "Use scripts/generate-secrets.sh to generate production secrets."
exit 1
